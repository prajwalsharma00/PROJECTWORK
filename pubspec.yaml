name: todo_app
description: A Flutter todo list app with server sync

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.18.1
  path_provider: ^2.1.1
  connectivity_plus: ^4.0.2

flutter:
  uses-material-design: true
  assets:
    - assets/config.txt